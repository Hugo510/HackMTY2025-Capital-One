{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "dist", "node_modules", ".git"],
  "execMap": {
    "ts": "ts-node --transpile-only"
  },
  "env": {
    "NODE_ENV": "development",
    "TS_NODE_TRANSPILE_ONLY": "true"
  },
  "signal": "SIGTERM",
  "delay": "250"
}
